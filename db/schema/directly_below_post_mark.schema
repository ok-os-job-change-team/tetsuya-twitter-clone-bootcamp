# frozen_string_literal: true

# -*- mode: ruby -*-
# vi: set ft=ruby :
create_table :directly_below_post_marks, id: false, primary_key: %i[comment_id], charset: 'utf8mb4',
                                         collation: 'utf8mb4_bin', comment: 'ポスト直下のコメント', force: :cascade do |t|
  t.references :comment, null: false, foreign_key: true, comment: 'コメントID'
end
