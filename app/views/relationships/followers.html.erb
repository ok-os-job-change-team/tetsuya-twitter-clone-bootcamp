<% provide(:title, 'フォロワー一覧') %>

<div class="contents-wrapper">
  <h1>フォロワー</h1>
  <%= render 'layouts/flash' %>

  <% if @users.exists? %>
    <%= render 'users/list', users: @users %>
  <% else %>
    <p>フォロワーはいません</p>
  <% end %>
</div>
