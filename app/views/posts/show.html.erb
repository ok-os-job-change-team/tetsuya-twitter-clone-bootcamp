<% provide(:title, 'ポスト' + @post.title) %>
<%= render 'shared/header' %>

<%= render 'layouts/flash' %>

<h2><%= @post.title %></h2>
<p><%= @post.content %></p>
<p><%= link_to @post.user.email, user_path(@post.user) %></p>
<p><%= link_to 'ポスト一覧', posts_path %></p>